spring:
  application:
    name: fellowship
  cloud:
    config:
      uri: http://localhost:8888/fellowship
      label: local
      fail-fast: true
      enabled: true

#encrypt:
#  key: IMSTMMETRIC

#  "spring.cloud.config.enabled=true",
management:
  endpoint:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: *
#  "management.endpoint.env.post.enabled=true", "management.endpoints.web.exposure.include=env, refresh" },
