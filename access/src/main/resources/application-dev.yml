spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://52.79.37.172:8888/
      label: develop
      fail-fast: true
      enabled: true
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
